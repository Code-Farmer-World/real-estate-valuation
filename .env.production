# 正式環境的後端 API。
# Vite 的 import.meta.env 是 build 期的字串替換、不是執行期讀取，所以這個值在
# docker build 當下就烤進 bundle 了。換後端位址要重 build image，改 compose
# 的環境變數沒有用。
VITE_API_URL=https://real-estate-valuation-api.pamis.dev

# 不設 VITE_LOG_LEVEL：axiosService 只看有沒有值，有值就把日誌調成 info。
# 正式環境維持 warn，不要把每一筆請求都印到 console（見 src/services/axiosService.ts）。
