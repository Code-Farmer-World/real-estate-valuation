# 版本紀錄

## 未發布

### 系統導覽

畫面上有不少「不解釋就會被誤讀」的設計決策——自我驗證為什麼排在最前面、
個別因素以 0 計的前提、優劣等級 116 項為什麼是 232 格、審查發現與輸入值疑慮
為什麼要分開看。這些原本只寫在 README、CHANGELOG 與程式碼註解裡，
第一次打開畫面的人看不到。現在做成系統內建導覽。

右下角浮動按鈕列出三份導覽：新手上路、產出模式、審查模式。導覽**不會自動跳出**，
需要時自己點開，清單標示哪幾份還沒看過。需要先上傳檔案的那兩份會停用並寫明缺什麼，
而不是讓人點了才卡在等不到的錨點上。

導覽內容是資料不是程式碼：在 `src/tours/` 新增一份 JSON 就自動收錄，不必改
`.ts` 或 `.vue`。定義檔在載入時做執行期驗證，格式錯了開發環境直接拋錯、
正式環境略過該份——輔助功能的設定錯誤不該讓主功能開不起來。

做法參考 `nexus-admin-frontend` 已在用的那一套（driver.js ＋ JSON 定義 ＋
錨點 e2e 防呆），依本專案的差異調整：那邊用路由切頁，這裡只有一個路由，
改成切模式與分頁；那邊用權限決定誰看得到，這裡沒有登入，改成看有沒有上傳資料。

### 這一版的取捨

- **`mode` 與 `tab` 從 HomeView 的區域 ref 提升到 `stores/ui.ts`**。導覽要能
  「先切到該模式再指元素」，狀態有了第二個消費者就該提升。順帶把原本在 script
  與 template 各寫一次的字面量聯集收成 `types/ui.ts` 的具名型別。
- **步驟多了 `optional` 欄位**，參考專案沒有。這裡畫面上不少區塊掛在 `v-if` 上，
  有沒有完全看資料（沒有不符之處就沒有「審查發現」區）。找不到錨點時整步略過，
  比顯示一個指著空白的泡泡好。
- **沒有加 `@vueuse/core`**。參考專案用它的 `useStorage` 記「已看過」，
  為了一個 API 加相依套件不划算，直接用 `localStorage` 包 try/catch
  （無痕視窗會丟例外）。新增的相依套件只有 `driver.js`。
- **`.modes` 加了 `width: fit-content`**。外觀沒變（本來就沒有底色與框線），
  但導覽遮罩是照元素實際範圍挖洞的，撐滿整列會框住一大片空白。

### 防呆

錨點是這種功能最容易壞、又最不容易被發現的地方：找不到錨點時導覽只會默默
退化成置中泡泡，不會報錯。所以擋兩層：

- `src/__tests__/tours.spec.ts` 掃原始碼確認每個錨點都還宣告著，
  並驗定義檔的整體性質與「已看過」紀錄的讀寫；`SurveyPanel.spec.ts`
  另外用假資料確認產出模式那七個錨點真的渲染得出來。不需要後端。
- `e2e/tour.spec.ts` 把導覽真的走一遍，確認每個錨點被高亮且在可視範圍內。
  需要資料的兩份沿用既有慣例，環境不足就跳過而不是紅掉。

兩者都驗過會因為改名或誤刪而失敗，不是寫了不會動的測試。

### 驗證

```
npm run type-check                              # vue-tsc
npx vitest run                                  # 31 passed（原本 16 + 新增 15）
npx tsc --noEmit -p e2e/tsconfig.json
npx oxlint .                                    # 0 問題
npm run build                                   # production build
npx playwright test --project=chromium          # 5 passed、5 skipped（需後端或 PDF fixture）
```

## v1.0.0（2026-09-13）交件版

2026 新北市 AI 智慧城市黑客松地政局命題的前端交件版本。
後端在 [`real-estate-valuation-py`](https://github.com/Code-Farmer-World/real-estate-valuation-py)，
版本紀錄與完整說明在那邊的 `CHANGELOG.md`。

### 做了什麼

兩種模式各自獨立，在畫面上是兩個分頁。

**產出模式**（預設）上傳填好的表3 地價區段勘查表 xlsx，畫面依序呈現
自我驗證結果、個別因素以 0 計的前提、回填格數、表4 的價格與權重、
87 列依據鏈、以及可下載的七個檔案。依據鏈的每一列都能點開，
看判級依據、矩陣查表結果與評價基準明細表頁碼。

**審查模式**上傳已填好的查估書表 PDF，比對三張表並列出不符之處。

畫面上呈現的順序是刻意的：自我驗證放最前面，因為它決定下面的數字能不能信。

### 技術

Vue 3.5 ＋ TypeScript ＋ Vite 8，Pinia 管狀態，axios 打 API。
測試用 Vitest 與 Playwright。

兩種模式不共用資料形狀。審查模式的型別對應金山範本的 `表1`／`表5-2`，
產出模式走 `表3`／`表5-1`，資料結構也不同。刻意不統一，
因為審查那條路徑對金山範本逐格零不符、完整可用，改了會弄壞。

### 驗證

```
npm run type-check                              # vue-tsc
npx vitest run                                  # 16 passed
npx tsc --noEmit -p e2e/tsconfig.json
npm run build                                   # production build
npx playwright test --project=chromium          # 4 passed（2 支需後端在 :8000）
```

`e2e/integration.spec.ts` 走完整條鏈：上傳勘查表，斷言畫面出現
自我驗證 10/10、總修正數、比準地比較價格 176,921、地價 177,000，
以及依據鏈的列點開後看得到判級理由。後端沒起就整支跳過。

`e2e/screenshot.spec.ts` 產 Demo 用的畫面截圖，不是驗證測試。

### 這一版的重要修正

- **端到端測試從沒真的跑過**。本機沒裝 Playwright 瀏覽器，
  所以沒人發現 `vue.spec.ts` 在改成兩種模式分頁之後就失效了。
  裝上 chromium 之後補了真實瀏覽器的整合測試。
- **標題原本叫「不動產估價案件審查」**，但預設進的是產出模式，
  一進來看到「審查」會以為走錯地方。改成「不動產估價查估書表」。
- **格數顯示**。畫面原本寫「優劣等級 116 格」而交付檔案實際動 232 格，
  因為優劣等級在書表上是兩欄。改成「116 項」加註「級數與等級文字共 232 格」。

### 沒做的

後端的 API 沒有認證，部署前要補。前端目前沒有登入流程。
